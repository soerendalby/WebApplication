@*
  Shared alerts and validation summary partial
*@
<div>
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    @if (TempData["Alert"] is string msg)
    {
        <div class="alert alert-info" role="alert">@msg</div>
    }
</div>
